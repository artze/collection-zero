const canvasSketch = require("canvas-sketch");
const P5 = require("p5");

const settings = {
  dimensions: [2048, 2048],
  p5: P5,
  animate: false
};

const sketch = ({ p5 }) => {
  /**
   * p5 setup()
   */
  return () => {
    /**
     * p5 draw()
     */
  };
};

canvasSketch(sketch, settings);
